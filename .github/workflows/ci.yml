name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt -r requirements-dev.txt

      - name: Lint
        run: ruff check .

      - name: Test
        run: pytest

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Push to HF Space (orphan — no binary history)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config user.email "ci@github-actions"
          git config user.name "GitHub Actions"
          git checkout --orphan hf-deploy
          git add -A
          git commit -m "Deploy from ${{ github.sha }}"
          git remote add hf-space https://kawas8516:$HF_TOKEN@huggingface.co/spaces/kawas8516/chat-cooking.git
          git push hf-space hf-deploy:main --force
